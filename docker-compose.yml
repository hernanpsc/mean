version: '3.8'

services:
  angular:
    hostname: localhost
    # container_name: front-prepagas
    build: front-prepagas
    ports:
      - "8080:80"
  express:
    build: server
    ports: 
      - "3000:3000"
    links:
      - database
  database:
    image: mongo
    ports:
      - "27017:27017"
